trigger:
- none
pool:
  vmImage: ubuntu-latest
steps:
- script: |
   whoami
   echo $(Pipeline.Workspace)
   echo $(Build.SourceBranchName)
   
   ls -ltra
  displayName: 'testing' 
- script: | 
  
   mvn install
   
   ls -ltra
   echo maven build completed
   
